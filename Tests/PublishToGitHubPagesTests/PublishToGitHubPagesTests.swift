import XCTest
@testable import PublishToGitHubPages

final class PublishToGitHubPagesTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PublishToGitHubPages().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
